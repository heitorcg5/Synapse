package com.synapse.modules.knowledge.entity;

/**
 * Types of links between knowledge items (graph / Zettelkasten).
 */
public enum KnowledgeRelationType {
    RELATED,
    DEPENDS_ON,
    EXTENDS,
    REFERENCE
}
